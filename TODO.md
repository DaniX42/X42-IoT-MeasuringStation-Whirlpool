# TODO

## High Priority

- Define the first functional scope for sensor data acquisition.
- Finalize ESP-WROOM-32 sensor pin mapping and wiring table.
- Calibrate all three SCT013 channels against a reference current meter.

## Medium Priority

- Add linting, formatting, and test tooling based on selected stack.
- Introduce CI checks for tests and documentation quality.
- Add environment-specific configuration templates in `config/`.

## Ongoing

- Keep `docs/decision-log.md` and `docs/memory.md` up to date.
- Update `CHANGELOG.md` for every meaningful change.
